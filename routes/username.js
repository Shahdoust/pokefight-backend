const express = require("express");

// const { usernamePost } = require("../controllers/postusername");

// const api = express.Router();

// api.route("/").post(usernamePost);

// module.exports = api;
